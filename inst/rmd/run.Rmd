---
output: 
    html_document:
        code_folding: hide
params:
    set_title: "Sourced Code"
    file:
        value: x
title: '`r params$set_title`'
date: '`r strftime(Sys.time(), format = "%B %d, %Y")`'
---

<!-- Allows wrapping in wide output --> 
<style>
pre {
  overflow-x: auto;
}
pre code {
  word-wrap: normal;
  white-space: pre;
}
</style>

```{r q0, echo = FALSE}
library(knitr)
read_chunk(params$file, labels="q0")
```
 
```{r q0, message=F, comment=NA}
```

```{r}
sessionInfo()
```